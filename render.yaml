# Render Blueprint: https://render.com/docs/blueprint-spec
# In the Render dashboard: New → Blueprint → connect this repo and apply.

services:
  - type: web
    name: amazon-ads-api-mock
    runtime: python
    plan: free
    region: oregon
    # Pin Python: 3.14 has no pydantic-core wheels yet → pip builds Rust → fails on Render (read-only cargo).
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /
    envVars:
      # If the build log still shows Python 3.14, add this manually in the service → Environment (then Clear build cache & deploy).
      - key: PYTHON_VERSION
        value: "3.11.9"
      # Used to sign JWT access tokens and report download tokens — change in dashboard if you rotate.
      - key: LWA_JWT_SECRET
        generateValue: true
      # Optional: force public URLs (custom domain). If unset, RENDER_EXTERNAL_URL is used automatically.
      # - key: PUBLIC_BASE_URL
      #   value: https://your-service.onrender.com
